# 开发环境（npm run dev 自动加载）

# 环境标识
VITE_APP_ENV='development'

# 开发环境接口前缀：vite dev 代理将该前缀转发到本地后端（见 vite.config.ts）
VITE_APP_BASE_API='/dev-api'

# 独立对话门户（v5ai-ui-chat）开发地址；未配置则「系统信息」页不显示入口
VITE_CHAT_UI_URL='http://localhost:5174'
