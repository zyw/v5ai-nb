# v5ai-nb Platform Context

The v5ai-nb 平台统一管理模型、Agent、知识库（RAG）、MCP Server、Skill、会话与运行记录。本上下文收录平台内独有的领域术语。

## Skill（技能）

**Skill**:
平台内可被 Agent 绑定并在运行时注入 Workspace 的可复用能力包。
_Avoid_: 技能包（指压缩包文件形态）、Application（历史遗留名）

**SKILL.md**:
每个 Skill 的入口定义文件，带 YAML Front Matter（`name`、`description` 必填非空）+ 正文；缺失时该 Skill 无法发布/注入。
_Avoid_: README、说明文件

**Skill 包**:
Skill 的 zip 文件形态（SKILL.md + prompts/ + resources/），通过上传或在线编辑产生。
_Avoid_: 技能压缩包（口语）

**版本（Version）**:
Skill 的可发布快照，状态三态：DRAFT（编辑中，唯一可编辑）/ PUBLISHED（已发布，运行时注入「当前版本」）/ OFFLINE（已下线，内容冻结不可编辑，可重新发布或删除）。
_Avoid_: 草稿版本号、发布版本（口语）

**当前版本（Current Version）**:
Skill 上指向运行时注入版本的指针；下线当前版本会清除该指针，Skill 随即停止注入、不可再被绑定。

**上线 / 下线**:
发布（DRAFT/OFFLINE → PUBLISHED）与撤下（PUBLISHED → OFFLINE）版本的操作；Skill 存在任何已发布版本时不可禁用/删除，须先全部下线。
_Avoid_: 回滚（回滚为兼容保留的旧接口，不再作为版本管理主流程）

**AI生成**:
按「需求说明」起草 SKILL.md 内容（含合法 frontmatter）的 AI 操作，只作用于 SKILL.md 文件。
_Avoid_: 自动生成（避免与其它自动生成混淆）

**AI优化**:
按「需求说明」（必填）+「优化方向」（选填）重写当前打开文件的 AI 操作。
_Avoid_: AI 改写（与优化同义，取其一）

**模型对话（Model Chat）**:
平台按已配置的 Chat 模型发起的一次文本补全调用（输入消息列表，返回完整文本或首个响应块）。
_Avoid_: 大模型调用、LLM 请求（口语）

## Resource（资源）

**访问URL（access_url）**:
资源的稳定逻辑引用，用于访问/展示资源内容；始终为应用内鉴权路径，与存储后端（本地磁盘 / MinIO）、桶私有策略或签名有效期无关。
_Avoid_: 对象直链、MinIO 裸 URL、预签名 URL（「具体定位符」是与稳定引用不同的概念）

**图标（Icon）**:
供应商的可选视觉标识；取值为资源 `access_url`（应用内路径）或外部网络 URL，通过「上传 / 资源库单选（AVATAR）/ 网络地址」三种方式设置。
_Avoid_: Logo 文件、图标文件（指存储介质，而非「选择后的值」）

## 授权（客户端）

**客户端标识（clientid）**:
登录时由客户端提供的身份标识，写入令牌；此后每个请求都须在请求头或参数中回传同一个值，不一致即视为无效令牌。它同时界定该客户端的接口访问路径与 IP 白名单作用域。
_Avoid_: 客户端ID、clientId（指同一概念的其它写法时请统一用本词条）

## Agent（智能体）

**Agent（智能体）**:
平台统一管理的可运行 AI 单元：绑定一个对话模型，可选绑定知识库、MCP Server 与 Skill，有草稿/已发布/已禁用三态；对外以 Agent Key 标识，经 API Key 鉴权调用。代码中平台实体类为 `Agent`、运行时契约 record 为 `AgentDTO`——两者是同一概念的持久化形态与运行形态，不是两个概念。
_Avoid_: 应用、Application（V8 迁移前的历史名，见 AGENTS.md「注意历史遗留命名」）

**Agent Key**:
Agent 在平台内唯一的对外运行标识，用于运行 API 路径、API Key 归属与各类绑定关系；创建后不可修改。
_Avoid_: App Key、appKey（界面残留旧文案，统一用本词条）

**会话消息（SessionMessage）**:
一次会话中的一条消息，带所属会话与 Agent、角色（用户 / 助手）与内容，以及思考（仅助手）、引用（仅助手）、附件与用量；对话历史与记忆回放都按会话把它组织起来。
_Avoid_: 聊天记录、对话条目（指整段会话或界面列表，而非「一条消息」）

**运行（Run）**:
一次 Agent 执行：入参是运行请求（`AgentRunBo`：Agent Key + 会话 + 提问 + 可选 RAG 上下文与历史），产物是运行结果（`AgentRunVo`）与运行事件流（`RuntimeRunEventDTO`）；每次运行有唯一运行 ID，运行事件、MCP 调用审计与用量都按它归属。
_Avoid_: 请求、调用（口语；「运行」是平台内一次完整执行的单位，一个会话包含多次运行）

**会话（Conversation）**:
平台内一段连续对话的容器：以会话 ID 标识，汇集该话题下的多条会话消息与多次运行；服务端持久化，可命名、可归档。会话由创建它的调用方归属——门户侧以 API Key 为归属主体（同一 Key 的会话互相可见，不同 Key 之间隔离），调试入口产生的会话不属于任何 Key。会话 ID 由调用方生成并在同一话题内复用。会话在**创建时**就由首条提问取名（见「会话名称」），门户列表因此只读会话表。
_Avoid_: 聊天记录、对话条目（指界面列表或一整段消息，不是这个容器）；把「会话」与「会话消息」混用（前者是容器，后者是其中一条）。

**会话名称（会话标题）**:
会话在门户列表里显示的名字，两级生成、都在服务端：会话创建时取**首条提问**的首行（压缩空白后截到 100 字）作为兜底名，首轮回答落库后再异步调一次模型把它改写成不超过 30 字的短标题（用哪个模型见「次要模型」）。名称带**来源标记**（`v5ai_conversation.name_source`）：只有 `AUTO` 才允许模型改写；用户一旦在门户改名，标记即为 `USER`，此后任何自动命名都不再覆盖——用户输入永远优先。提问为空的会话（如只发了图片）保持未命名，由门户显示占位「新会话」。
_Avoid_: 预览、首条消息摘要（那是旧实现里读取侧每次现查消息表的兜底做法，已随本次改造删除）。

**会话历史窗口（历史窗口）**:
一次运行最多把多少同会话历史发给模型的预算（`v5ai.agentscope.history`，默认 40 条 / 8000 字符 / 保底 2 条）：按「最近优先」保留，裁剪后首条一定对齐到用户提问（不留没有提问的回答）。显式 `resume` 传入的历史同样过这道窗口——否则它就成了绕过预算的后门。窗口只约束文本，图片另有 16MB 字节预算。
_Avoid_: 上下文窗口、模型窗口（那是**模型**的硬限制，平台侧这道裁剪只是为了不撞上它）；「截断」（本机制保留的是最近一整段，不是把某条消息截半）。

**会话摘要**:
历史窗口之外的旧内容被压缩成的一段文本（`v5ai_conversation_summary`，一会话一行、按水位 `covered_until_message_id` 滚动覆盖）：它随系统提示注入，让模型「记得」已经滚出窗口的更早对话。生成异步、失败静默、不记用量（与「会话名称」同一性质）；一旦「重新生成」作废的范围落在它覆盖的水位之内，整条摘要作废、下次运行按当前有效历史重建。
_Avoid_: 上下文压缩、记忆（前者是 AgentScope 自带能力的名字、本项目未启用；后者在本平台专指跨会话记忆，尚未实现）。

**次要模型（Secondary model）**:
Agent 上**可选**配置的一个模型（`v5ai_agent.secondary_model_id`），供两处**平台内部**的模型调用使用：会话名称改写与会话摘要压缩（`AuxiliaryModel` 解析、两个 Writer 消费）。留空即**回退该 Agent 绑定的对话模型**——「次要模型 → 对话模型」两级回退，没有第三层，也不存在全局配置项（原 `v5ai.chat.conversation-summary.model-id` 已随本次改动下线）。必须是**已启用的 CHAT 类型**模型，保存时校验；这两条调用都**不记用量、不落事件**，用的是哪个模型只能靠 debug 日志归因。它随**发布快照**固化，换模型须重新发布才对门户生效。见 `docs/adr/0010-agent-secondary-model.md`。
_Avoid_: 小模型、辅助模型（口语，未表达「挂在 Agent 上、两级回退」这一结构）；也不要把它与 Agent 绑定的「对话模型」并列称作「主模型 vs 次模型」——回退链的另一端就是那个对话模型本身，不存在第三个「主模型」。


**归档（归档会话）**:
会话的收尾态：归档后从会话列表默认隐藏，且**不可再继续对话**，但消息与运行记录全部保留；可取消归档恢复为进行中。与删除不同，归档不丢弃任何数据。
_Avoid_: 删除、隐藏（「隐藏」不含"禁止继续对话"这一后果，而这是本平台刻意的取舍）

**附件（Attachment）**:
随一条会话消息提交、供模型输入的**图片**：先上传为资源（`plm_resource`，`biz_type=ATTACHMENT`）再以资源 ID 随消息提交；只有用户消息可带附件，且仅当该 Agent 所绑 CHAT 模型声明支持图片输入时可用。附件属于消息，不属于知识库。
_Avoid_: 文件、上传物（泛指，未表达"随消息提交给模型输入"这一用途）；与知识库「源文件」混同（后者 `biz_type=DOCUMENT`，供检索，不随消息）

**支持图片输入（capabilities 含 image）**:
CHAT 模型配置（`v5ai_model.config` 里的 `capabilities` 数组）中的一项声明，表示该模型可接受图片输入；含 `image` 即视为支持。它同时是附件能力的唯一开关：Agent 所绑 CHAT 模型未声明该项时，门户禁用上传入口、接口拒绝携带附件的请求（400）。
**判定时机（两个容易踩的点）**：判定的对象是「该 Agent 当前解析出的 `modelId`」——线上门户取**发布快照**里的 `modelId`（`PublishedAgentResolver.resolve`），管理端调试取实时编辑行（`resolveForDebug`）；而 `capabilities` 本身按模型 ID **实时读**。因此：给模型开启/关闭本项，对已发布的 Agent **立即生效、无需重新发布**；但**给 Agent 换了绑定模型后必须重新发布**，否则门户仍按旧快照的模型判定。另外门户的 `imageSupported` 只在进入页面时随 bootstrap 取一次，改完配置需**刷新页面（或重登）**才会重新取到。
_Avoid_: 视觉模型、多模态模型（前者与 `defaultVisionModel`「默认视觉模型」撞词，后者泛指模型能力而非本平台的这一声明项）；用 `vision` 表示本项（既有代码与管理端统一写 `image`）

**停止（停止对话）**:
调用方对一次进行中运行的收尾动作：服务端终止该次运行、把运行标记为**已取消**，并保留已生成的部分回答（不丢弃调用方的提问）。与「运行失败」不同——停止是调用方主动且正常的收尾，不是异常。
_Avoid_: 中断、终止、取消（口语混用）；与「重新生成」混同（那是丢弃回答后重跑，属另一个动作）

**Agent 调试（预览与调试）**:
在管理端直接跑一次 Agent 对话、不签发 API Key 的调试入口。**草稿态可调试**（便于发布前验证），**仅已禁用态拒绝**；与知识库「知识问答」调试的关键差异是**它走持久化运行时**：会话、消息、运行记录、运行事件、Agent 状态都会落库——思考随之落在所属助手消息上，但**不写运行事件表**（见「思考」）。知识库问答调试则整体不持久化（不写运行记录）。
_Avoid_: 试运行（口语）、把两者的持久化口径混用（一边落库一边不落，是刻意设计而非疏漏）

## 知识库（RAG）与调试

**知识库详情页（Knowledge Base Detail）**:
从知识库列表点击库名进入的管理页；头部固定展示库标识（名称 / ID / 嵌入模型 / 更新时间），其下「原始文档 / 切片详情 / 知识检索 / 知识问答」四个 tab 均围绕该知识库操作。
_Avoid_: 文档详情页（指单个文档的页面，是另一层级概念）

**原始文档（tab）**:
知识库详情页中「库内文档集合」视图的命名（沿用参考产品）：文档导入、状态、删除等文档级管理入口所在。
_Avoid_: 理解为「文档原文预览」（原文内容未在本页提供）

**重新解析（重新索引）**:
对文档重新执行「解析 → 切片 → 嵌入 → 向量入库」的幂等重建——把该文档的索引任务重置为 PENDING 由 Worker 重跑（Worker 会先清旧切片/向量）。批量版仅对「处理完成 / 处理失败」的文档生效，待处理/解析中/处理中的文档跳过。
_Avoid_: 与「重试」混用（重试仅针对单文档的处理失败）；与「删除」混淆（删除清数据，重新解析重建数据）。

**源文件（Source File）**:
知识库文档的原始文件（上传的文件字节 / URL 抓取到的网页内容），统一存于通用资源存储（`plm_resource`，`biz_type=DOCUMENT`）。文档行以 `resource_id` 指向该资源；`content`（BYTEA）列仅作存量兜底，不再写入。资源被删除后文档仍保留，但预览 / 下载 / 重新解析不可用。
_Avoid_: 原文内容（指解析出的可检索文本，与源文件字节不同）；BYTEA 内容（是存量实现细节，非概念）

**切片（Chunk）**:
入库后可按内容检索的最小单元，归属于某一文档，由解析切分或手工新增产生；可单独查看 / 编辑 / 删除，内容改动后须重新嵌入。
_Avoid_: 片段、文本片、段落（界面文案中的混用词，均指切片及其内容）

**手工切片（Manual Chunk）**:
在「切片详情」中由人工新增的切片，与解析切分产生的切片并列，内容不必来自所属文档的原文；序号在所属文档内顺延，编辑只改内容、不改变所属文档。
_Avoid_: 自定义切片、手动分块、新增片段

**所属文档（Owning Document）**:
每个切片唯一归属的文档；它决定切片序号的取值空间，并在检索命中时作为来源标注，且必然与切片同属一个知识库。切片建立后所属文档不可更改。
_Avoid_: 归属文档、来源文档（界面、后端异常文案与 VO 注释中的混用词，统一用本词条）

**切片策略（Slice Strategy）**:
知识库文档切分方式的选择（按长度 / 按分隔符 / 正则 / 智能），持久化为 `config.chunkParams.sliceStrategy`；每个策略对应一个实现类，由注册中心按策略路由。
_Avoid_: 切分模式、切片模式（旧 `mode` 字段名的口语残留）

**候选片段（Segment）**:
切片策略一级切分（按空行 / 分隔符 / 正则）的产物，随后经统一流水线按最大长度切分为最终切片；与「切片」不同，候选片段尚未定长。
_Avoid_: 段落（界面与代码注释中的口语词，指候选片段；不是切片）

**调试参数**:
知识检索 / 知识问答页的检索与问答参数，分两组：检索参数（结果返回数量、问题改写、重排、阈值过滤、融合策略及其参数）与模型回答参数（对话模型、拼接邻近文本片数量、Prompt 模板，模板可插入 `<Documents>` 变量引用 RAG 命中）。初始值取自知识库既有配置；页面改动经自动保存（`PUT .../config`）写回为该知识库的默认配置，而调试请求（`retrieve` / `chat/stream`）本身不回写——请求空字段按 请求 → 知识库配置 → 内置默认 依次补齐。
_Avoid_: 会话级参数、仅本次会话生效（页面改动会自动保存为知识库默认值，并非一次性会话状态）

**融合策略（Fusion Strategy）**:
知识检索结果如何由两路检索通道合成的策略选择，取值：`RRF`（倒数排名融合，默认）/ `WEIGHTED_SUM`（加权求和）/ `VECTOR`（仅向量）/ `KEYWORD`（仅关键词）——后两者是"不融合"的退化选择。参数：RRF 用 RRF K 值，WEIGHTED_SUM 用向量权重（关键词权重 = 1 − 向量权重）。策略与参数作用于调试检索与运行时检索：运行时仅当绑定知识库全部显式配置一致时执行融合，否则走默认「向量优先 + 关键词兜底」路径。
_Avoid_: 检索策略、通道选择（这两个词描述"跑哪些子检索器（向量 / 关键词）"，与"结果如何合成"不同维度，勿混用）

**知识检索 / 知识问答（调试 tab）**:
针对单个知识库的 RAG 命中预览（检索）与多轮会话问答（问答）；与 Agent 运行期会话不同，问答调试会话不持久化运行记录。其事件流为 RETRIEVAL（引用）→ REASONING_DELTA*（思考）→ TEXT_DELTA*（回答），**与 Agent 调试同构**，故两处共用同一套渲染组件。

**思考（Reasoning）**:
模型在给出回答之前产生的推理内容，与回答**分流**推送（事件 `REASONING_DELTA` / 内部 `AgentTextEvent.Reasoning`），界面折叠展示、思考期间标题为「思考中…」、回答首个片段到达时自动收起一次。思考**随所属助手消息持久化**（供门户回看；被停止或失败时连同半截一并保留），但**不回放给模型**——不进上下文、不占历史窗口预算、也不进会话摘要；运行事件表（`v5ai_run_event`）仍然不落思考（避免事件表随思考长度膨胀）。
_Avoid_: 推理过程（代码标识符保留 reasoning/REASONING_DELTA，界面与领域口径统一用「思考」）

**引用（Citation）**:
一次 RAG 检索注入系统提示的那些切片，及其对外展示形态：文档标题 + 切片序号 + 相似度 + 截断后的切片内容。Agent 调试与知识库问答的引用载荷**同构**（同一组字段名、单条内容同样截断 2000 字符），因此共用同一个解析器与渲染组件；载荷不带来源知识库名（只带 knowledgeBaseId）。引用**随所属助手消息持久化**（`v5ai_message.metadata`，`{"citations":…}`，存的是当时载荷的快照；同一轮检索到多次时按切片保序去重，实时与回看一致），供门户回看；**不回放给模型**——不进上下文、不占历史窗口预算、也不进会话摘要（与「思考」同口径，见 docs/adr/0009-citations-persisted-on-assistant-message.md）。
_Avoid_: 命中（指检索结果本身，偏实现；对外一律称引用）、引用的切片（把展示形态与它承载的切片混淆）

**引用展示（Citation display）**:
Agent 上「聊天窗口是否展示引用」的开关（`v5ai_agent.show_citations`，默认开），随发布快照下发门户，决定助手消息下方那个「引用（N 条）」折叠块**渲染不渲染**。它是**纯展示**开关：检索照常发生、`RETRIEVAL` 事件照常推送、引用照常随消息落库、历史接口照常返回——关掉只是不画（引用是"这条回答当时依据了什么"的快照，事后无法重建，所以开关不能是破坏性的，见 `docs/adr/0011-citations-toggle-is-display-only.md`）。**同时作用于管理端「预览与调试」**（调试面走实时编辑行，因此改完立刻能看到显隐，不必先发布）；知识库「知识问答」没有 Agent 上下文，不受影响、照常展示。
_Avoid_: 引用开关（易被读成"关掉就不检索/不落库"）；与「知识库是否启用」（`ragEnabled`）混为一谈——那是**要不要检索**，这是**要不要画出来**，两者独立。

**RAG 调用方式（rag_call_mode）**:
Agent 决定「知识库检索何时发生」的设置，两态：智能调用 / 强制调用；仅在 Agent 启用知识库（ragEnabled）时生效，缺省为强制调用。
_Avoid_: 检索模式、RAG 模式（口语）；把「调用方式」与「是否启用知识库」混为一谈（前者只在后者开启时才有意义）

**智能调用**:
知识库检索的时机与范围由 LLM 在对话中自主判断——可检索、可不检索、也可多次检索（平台把可用知识库告知模型，由模型决定）。
_Avoid_: 按需检索、自动检索；误解为「关闭知识库」（关闭是 ragEnabled=false，与调用方式无关）

**强制调用**:
每次对话都在模型生成回答前检索一次知识库并注入上下文，检索结果固定进入系统提示；这是平台原有的知识库行为。
_Avoid_: 固定检索、总是检索（口语）

**文档导入方式（文档文件类型）**:
三种导入入口：本地上传 / URL 导入 / 云存储（OSS·S3，未实现占位）。文档文件类型（`DocumentFileType`）即解析器能力边界：TXT / Markdown / PDF / DOCX / XLSX / PPTX / HTML / CSV，另有 `URL` 仅作存量行兼容。URL 导入按「URL 扩展名 → Content-Type → 网页 HTML」判定实际类型后分发对应解析器。
_Avoid_: 界面列格式但后端无对应解析器；把 `URL` 当作「网页」本身（网页现判定为 HTML 类型，`URL` 只是历史遗留）；把导入方式与文件类型混同（`sourceType`=UPLOAD/URL 与 `fileType` 正交）。

**被引用（Referenced）**:
知识库存在至少一条 Agent 绑定（`v5ai_agent_knowledge`，任意 Agent 状态均计入）的状态。被引用的知识库**不可删除**（须先解除全部绑定）；**禁用**仅警告不阻断（禁用后已绑定 Agent 的检索停用）。启用不受影响。
_Avoid_: 占用、被占用（口语）；混淆「被引用」与文档/切片/任务这类「子资源」——子资源删除时级联清理，不构成引用。

**重新生成（Regenerate）**:
对话窗口内对某条回答的交互操作：丢弃该回答及其之后的全部消息，用同一条提问重跑一次；新回答就地替换原回答，不保留多版本。**门户侧（`v5ai-ui-chat`）是服务端操作**：`POST …/chat/conversations/{id}/regenerate` 把该轮提问之后的消息标记为「作废」，并**复用那条提问**重跑——因此历史里不会出现两条相同的提问，作废的回答也不再进入模型上下文。知识库问答与 Agent 预览调试面板仍是前端行为（前者由客户端回放历史重发，后者每问都是独立会话、服务端不累积上下文）。
_Avoid_: 重试（指失败后的重发，语义不同）、重新回答；重新解析/重新索引（那是知识库文档的索引重建）

**作废（作废消息）**:
被「重新生成」替换掉的消息所处的状态：`v5ai_message.superseded_at` 非空即为此态。数据全部保留（可审计、可反悔、附件引用不断），但**不参与展示与记忆回放**——这是**软**替换而不是删除，好处是「消息序列」与「已计费用量」始终对得上。作废范围是该轮提问**之后**的全部消息（含被替换的回答与更晚的整轮问答）；锚点提问本身保持有效，因为重新生成复用它。
_Avoid_: 删除、隐藏（「隐藏」不含「不再进入模型上下文」这一后果）

## 知识库（RAG）向量存储

**嵌入模型（Embedding Model）**:
提供文本向量化的 EMBEDDING 类型模型；知识库绑定其一，索引与检索均按该模型产出向量。
_Avoid_: 向量模型（口语）

**向量存储实例（存储实例）**:
平台管理的向量数据库连接（pgvector / Milvus / Elasticsearch 之一），知识库必须显式绑定；向量按实例承载，与业务库解耦。
_Avoid_: 向量库（口语）、默认实例（不存在隐式默认）

**向量集合（Collection）**:
单个知识库在一个存储实例内的向量承载单元：pgvector 下为按该库嵌入维度建的表，Milvus 下为 collection，Elasticsearch 下为 index。
_Avoid_: 向量表（pgvector 特有叫法）

**向量标识（vector_id）**:
业务为每个切片生成的稳定 UUID，切片行与其在向量集合中的向量共用该标识（集合主键）。
_Avoid_: 切片自增主键（chunk.id，非跨库稳定标识）

**嵌入维度**:
知识库冻结的向量输出维度，来源于所绑嵌入模型的声明并在知识库上快照；维度变更须重建该库索引。
_Avoid_: 动态维度（随调用浮动）
